This example uses the Tailwind classes. In particular, the `wiggle` animation is configured as follows:

```javascript
theme: {
  extend: {
    animation: {
      wiggle: 'wiggle .5s ease-in-out infinite'
    }
  }
}
```
